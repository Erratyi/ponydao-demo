export function cents(value:unknown) { const s=String(value); if(!/^\d{1,10}(\.\d{1,2})?$/.test(s))throw new Error('金额须为正数，最多两位小数');const n=Math.round(Number(s)*100);if(!Number.isSafeInteger(n)||n<=0)throw new Error('金额必须大于零');return n; }
export function allocate(total:number,shares:{userId:string,bps:number}[]) { if(!Number.isSafeInteger(total)||total<=0)throw new Error('分账金额无效');if(!Array.isArray(shares)||!shares.length||new Set(shares.map(s=>s.userId)).size!==shares.length||shares.some(s=>!Number.isInteger(s.bps)||s.bps<=0)||shares.reduce((n,s)=>n+s.bps,0)!==10000)throw new Error('分配比例之和须为 100%，且成员不能重复');const rows=shares.map(s=>({...s,amount:Number(BigInt(total)*BigInt(s.bps)/BigInt(10000))}));let remainder=total-rows.reduce((n,s)=>n+s.amount,0);for(let i=0;remainder>0;i++,remainder--)rows[i%rows.length].amount++;return rows; }
